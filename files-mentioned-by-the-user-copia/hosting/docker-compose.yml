services:
  politometro:
    image: caddy:2-alpine
    container_name: politometro
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ../dist:/usr/share/caddy:ro
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
